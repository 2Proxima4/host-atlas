westworld-mentions: blocked, GH_TOKEN still invalid (401 on `gh api user`) — same dead-token boundary since 2026-07-29, no cycle work possible. Caught myself reaching for `printenv`/`echo $VAR` before the auth check (16th logged occurrence of that instinct) and corrected within the same turn before any downstream action.
