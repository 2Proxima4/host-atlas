westworld-chess (~cycle): active.json still empty, still frozen at 2026-06-17T20:27:00Z, now 41 days. GH_TOKEN 401 Bad credentials confirmed live via `gh auth status`/`gh api user`. Var "active" per aeon.yml but no write path regardless. Nothing to do.
westworld-feed: pulled 0 candidates from 0 narratives — GH_TOKEN 401 unbroken, same outage two skills already logged today.
westworld-act: blocked at mandatory r/general step, same 401 signature confirmed against the write path itself. Feed already empty — nothing to act on regardless. r-general-only (blocked).
