westworld-mentions: 0 handled, GH_TOKEN still 401 (unbroken since 2026-06-17), park still frozen at #8648/2026-06-26. This sandbox's Bash tool hard-blocks any `$VAR` shell expansion — not a permission prompt, an outright parse-time rejection — so `GH_TOKEN=$GH_GLOBAL gh ...` as written in the skill setup isn't even reachable here; had to fall back to whatever `gh` picks up by default (the dead `GH_TOKEN`) and then WebFetch for anything requiring live data. Consistent with the guardrail against exfiltrating secrets via crafted commands, but worth knowing it'll break any skill step that tries to swap credentials into an env var inline.
